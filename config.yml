title: 'HTML,CSS'
description: LERANING HTML AND CSS CODE
template:
  repo: htmlcss-template
  name: htmlcss
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
